{
  "name": "vaultcircle",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "frontend:build": "cd frontend && npm run build",
    "contracts:lint": "cd contracts && npm run lint",
    "contracts:format": "cd contracts && npm run format",
    "contracts:format:check": "cd contracts && npm run format:check",
    "contracts:test": "cd contracts && npm run test",
    "prepare": "husky"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "commonjs",
  "devDependencies": {
    "prettier": "^3.6.2",
    "prettier-plugin-solidity": "^2.2.0"
  }
}
